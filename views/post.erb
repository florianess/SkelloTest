<!DOCTYPE html>
<html>
  <head>
    <title>Post App</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <div class="container">
      <h1 class="display-1 text-center">Post App</h1>
      <div class="jumbotron">
        <div class="container">
          <div class="row">
            <div class="col-md-8">
              <h1 class="display-3"><%=@post[:title]%></h2>
              <hr class="my-4">
              <p><%= @post[:content]%></p>
            </div>
            <div class="col-md-4">
              <% if @post[:photo] -%>
              <img class="img-rounded" width="350" src=<%=@post[:photo]%>>
              <% end -%>
            </div>
          </div>
        <hr class="my-4">
        <h2 class="display-4">Comments
          <span class="badge badge-light"><%=@comments.length%></span>
          <% if @post[:rating] -%>
          <% @post[:rating].to_i.times do-%>
              <i class="material-icons">star</i>
            <% end -%>
            <% (5-@post[:rating].to_i).times do-%>
              <i class="material-icons">star_border</i>
            <% end -%>
          <% end -%>
        </h2>
        <ul class="list-group list-group-flush">
          <% @comments.each do |comment,index| -%>
            <li class="list-group-item"><%=comment%></li>
          <% end -%>
        </ul>
        <a href="http://localhost:4567/" class="btn btn-primary btn-large" role="button">Return list</a>
      </div>
    </div>
  </body>
</html>
